<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elmocorongo.greatdeals">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_redondo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_redondo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".RegistrosActivity"></activity>
        <activity android:name=".ProdutoUmActivity" />
        <activity android:name=".ProdutosActivity" />
        <activity android:name=".AtualizarNomeEmpresaActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".FuncionarioActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>